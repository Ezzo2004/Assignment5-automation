name: Ebay Scraper (Every 3 Hours)

on:
  schedule:
    -  cron: '0 */3 * * *'   # Run every 3 hours
  workflow_dispatch:        # Allows manual triggering

jobs:
  scrape-ebay:
    runs-on: ubuntu-latest
    env:
      TZ: America/New_York  # Set your local timezone if needed
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y chromium-browser
          pip install selenium pandas webdriver-manager fake-useragent nbconvert

      - name: Run Selenium scraper
        run: python Scrape.py
      
      - name: Run data cleaning script
        run: python clean_data.py
      
      - name: Run EDA notebook
        run: jupyter nbconvert --to notebook --execute EDA.ipynb --output EDA.ipynb


      - name: Commit and Push updated CSV
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add ebay_tech_deals.csv
          git add cleaned_ebay_deals.csv
          git add EDA.ipynb
          git commit -m "Updated Ebay tech deals data (3-hour intervals)"
          git push